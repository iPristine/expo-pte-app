import {
  enableStaticRendering as mobxEnableStaticRendering,
  observer as mobxObserver,
} from "mobx-react-lite"

export const enableStaticRendering = mobxEnableStaticRendering

export const observer = mobxObserver
